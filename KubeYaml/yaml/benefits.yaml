apiVersion: apps/v1
kind: Deployment
metadata:
  name: benefits-api
  namespace: nm
spec:
  replicas: 1
  selector:
    matchLabels:
      app:  benefits-api
      version: 11.0.0
  template:
    metadata:
      labels:
        app:  benefits-api
        version: 11.0.0
    spec:
      containers:
      - name: benefits-api
        image: emersso.azurecr.io/benefits:11.0.3
        resources:
          requests:
            memory: "32Mi"
            cpu: "25m"
          limits:
            memory: "64Mi"
            cpu: "100m"
        ports:
        - containerPort: 80
        env:              
        - name: "InsUrl"   
          value: "http://insurance-api-service"        
      imagePullSecrets:
      - name: topsecretregistryconnection
---
kind: Service
apiVersion: v1
metadata:
  name: benefits-api-service
  namespace: nm
spec:
  type: ClusterIP
  ports:
  - name: http
    protocol: TCP
    port: 80      
  selector:
    app:  benefits-api
    version: 11.0.0
